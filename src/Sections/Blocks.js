const TagsManager = require("../Core/TagsManager");

class Blocks extends TagsManager {
    constructor() {
        super();
    }

    stringify() {
        return super.stringify();
    }
}

module.exports = Blocks;
